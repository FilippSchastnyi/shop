import Button from '@mui/material/Button'
/*Types*/
import {ICardItem} from '../../../types'
import {FC} from "react";
/*Styles*/
import {Wrapper} from './FlyoutOption.styles'


const FlyoutOption: FC = () => {
    return (
        <div>FlyoutOption</div>
    )
}

export default FlyoutOption
